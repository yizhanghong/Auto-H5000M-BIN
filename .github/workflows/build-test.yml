name: Build ImmortalWrt H5000M
on:
  workflow_dispatch:
    inputs:
      enable_adguardhome:
        description: '启用 AdGuardHome'
        required: false
        default: true
        type: boolean
      enable_homeproxy:
        description: '启用 HomeProxy'
        required: false
        default: false
        type: boolean
      enable_openclash:
        description: '启用 OpenClash'
        required: false
        default: true
        type: boolean
      enable_mihomo:
        description: '启用 Mihomo'
        required: false
        default: false
        type: boolean
      enable_upnp:
        description: '启用 UPnP'
        required: false
        default: true
        type: boolean
      enable_vlmcsd:
        description: '启用 VLMCSd'
        required: false
        default: true
        type: boolean
      enable_smartdns:
        description: '启用 SmartDNS'
        required: false
        default: false
        type: boolean
      enable_mosdns:
        description: '启用 MosDNS'
        required: false
        default: true
        type: boolean
      enable_dockerman:
        description: '启用 DockerMan'
        required: false
        default: false
        type: boolean
      enable_qmodem_next:
        description: '启用 QModem Next (新版) - 与旧版互斥'
        required: false
        default: true
        type: boolean
      enable_qmodem:
        description: '启用 QModem (旧版) - 与新版互斥'
        required: false
        default: false
        type: boolean
      enable_mwan:
        description: '启用 MWAN (多WAN负载均衡)'
        required: false
        default: true
        type: boolean

permissions:
  contents: write

env:
  REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x-24.10
  REPO_BRANCH: mt798x-mt799x-6.6-mtwifi
  CONFIG_URL: https://raw.githubusercontent.com/padavanonly/immortalwrt-mt798x-6.6/refs/heads/mt798x-mt799x-6.6-mtwifi/defconfig/mt7987_mt7992.config
  SOURCE_DIR: immortalwrt
  TZ: Asia/Shanghai

jobs:
  # ============================================================================
  # Job 1: 环境准备 (5-10 分钟, 可并行)
  # ============================================================================
  prepare:
    runs-on: ubuntu-22.04
    steps:
      - name: 环境清理和准备
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          echo "🧹 [准备阶段] 开始清理环境..."
          df -h
          
          # 删除 Docker 镜像
          docker rmi $(docker images -q) 2>/dev/null || true
          docker system prune -f || true
          docker image prune -a -f || true
          docker volume prune -f || true
          
          # 删除不需要的组件
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android \
            /opt/ghc /etc/mysql /etc/php /usr/local/share/boost /usr/share/swift \
            /opt/hostedtoolcache /usr/local/.ghcup /usr/local/graalvm /imagegeneration 2>/dev/null || true
          
          # 卸载不需要的软件
          sudo -E apt-get -y purge azure-cli* docker* ghc* zulu* hhvm* llvm* firefox* \
            google* dotnet* aspnetcore* powershell* openjdk* adoptopenjdk* mysql* php* \
            mongodb* moby* snap* 2>/dev/null || true
          
          # 清理缓存
          sudo apt-get clean && sudo apt-get autoclean && sudo rm -rf /var/lib/apt/lists/* 2>/dev/null || true
          sudo rm -rf /tmp/* /var/tmp/* ~/.cache/* /var/log/* 2>/dev/null || true
          
          echo "✅ 环境清理完成"
          df -h

  # ============================================================================
  # Job 2: 下载源码和编译链 (20-30 分钟, 首次)
  # ============================================================================
  download:
    runs-on: ubuntu-22.04
    needs: prepare
    outputs:
      cache-key: ${{ steps.cache-key.outputs.key }}
      config-hash: ${{ steps.config.outputs.hash }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 生成缓存键
        id: cache-key
        run: echo "key=${{ github.run_id }}-${{ github.ref }}" >> $GITHUB_OUTPUT

      - name: 计算配置哈希
        id: config
        run: |
          # 计算配置文件的哈希值，用于判断配置是否改变
          CONFIG_HASH=$(cat h5000m.config feeds.conf.default 2>/dev/null | md5sum | cut -d' ' -f1)
          echo "hash=$CONFIG_HASH" >> $GITHUB_OUTPUT
          echo "📋 配置哈希: $CONFIG_HASH"

      - name: 恢复编译工具链缓存 (加速首次)
        uses: actions/cache@v4
        with:
          path: |
            ~/.ccache
            ${{ env.SOURCE_DIR }}/staging_dir/host
            ${{ env.SOURCE_DIR }}/staging_dir/toolchain-*
            ${{ env.SOURCE_DIR }}/staging_dir/target-*
          key: ${{ runner.os }}-toolchain-${{ env.REPO_BRANCH }}-v1
          restore-keys: |
            ${{ runner.os }}-toolchain-${{ env.REPO_BRANCH }}-
            ${{ runner.os }}-toolchain-

      - name: 克隆源码
        run: |
          echo "📥 [下载阶段] 克隆源码..."
          if [ -d "$SOURCE_DIR/.git" ]; then
            cd $SOURCE_DIR
            git fetch origin $REPO_BRANCH
            git reset --hard origin/$REPO_BRANCH
            git clean -fd
          else
            if [ -d "$SOURCE_DIR" ]; then
              echo "⚠️ $SOURCE_DIR 已存在且不是 git 仓库，先删除..."
              rm -rf "$SOURCE_DIR"
            fi
            git clone -b $REPO_BRANCH --single-branch --depth=1 $REPO_URL $SOURCE_DIR
          fi
          echo "✅ 源码克隆完成"

      - name: 设置 feeds
        run: |
          cd $SOURCE_DIR
          echo "🔄 更新软件源..."
          
          if [ -f "$GITHUB_WORKSPACE/feeds.conf.default" ]; then
            cp "$GITHUB_WORKSPACE/feeds.conf.default" ./feeds.conf.default
          fi
          
          # 更新 feeds，允许部分失败
          ./scripts/feeds update -a 2>&1 | grep -E "^(Scanning|Updating)" || true
          ./scripts/feeds install -a -f 2>&1 | tail -5 || true
          
          echo "✅ Feeds 设置完成"

      - name: 下载编译依赖 (会缓存)
        run: |
          cd $SOURCE_DIR
          echo "📦 [下载阶段] 下载编译依赖..."
          
          for attempt in 1 2 3; do
            if make download -j16 2>&1 | tail -10; then
              echo "✅ 依赖下载成功"
              exit 0
            fi
            echo "⚠️  第 $attempt 次下载失败，重试..."
            find dl -size -1024c -delete 2>/dev/null || true
            sleep $((attempt * 5))
          done
          
          echo "❌ 依赖下载失败"
          exit 1

      - name: 缓存下载的依赖 (关键!)
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.SOURCE_DIR }}/dl
            ${{ env.SOURCE_DIR }}/staging_dir/host
            ${{ env.SOURCE_DIR }}/staging_dir/toolchain-*
            ${{ env.SOURCE_DIR }}/staging_dir/target-*
          key: ${{ runner.os }}-downloads-${{ env.REPO_BRANCH }}-${{ steps.cache-key.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-downloads-${{ env.REPO_BRANCH }}-

  # ============================================================================
  # Job 3: 编译固件 (30-90 分钟, 首次; 5-30 分钟, 后续)
  # ============================================================================
  build:
    runs-on: ubuntu-22.04
    needs: download
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 恢复缓存
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.SOURCE_DIR }}/dl
            ${{ env.SOURCE_DIR }}/staging_dir/host
            ${{ env.SOURCE_DIR }}/staging_dir/toolchain-*
            ${{ env.SOURCE_DIR }}/staging_dir/target-*
          key: ${{ runner.os }}-downloads-${{ env.REPO_BRANCH }}-${{ needs.download.outputs.cache-key }}
          restore-keys: |
            ${{ runner.os }}-downloads-${{ env.REPO_BRANCH }}-

      - name: 恢复 ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{ runner.os }}-ccache-${{ env.REPO_BRANCH }}-v1
          restore-keys: |
            ${{ runner.os }}-ccache-${{ env.REPO_BRANCH }}-

      - name: 克隆源码
        run: |
          if [ ! -d "$SOURCE_DIR/.git" ]; then
            if [ -d "$SOURCE_DIR" ]; then
              echo "⚠️ $SOURCE_DIR 已存在且不是 git 仓库，先删除..."
              rm -rf "$SOURCE_DIR"
            fi
            git clone -b $REPO_BRANCH --single-branch --depth=1 $REPO_URL $SOURCE_DIR
          fi

      - name: 设置 feeds
        run: |
          cd $SOURCE_DIR
          if [ -f "$GITHUB_WORKSPACE/feeds.conf.default" ]; then
            cp "$GITHUB_WORKSPACE/feeds.conf.default" ./feeds.conf.default
          fi
          ./scripts/feeds update -a 2>&1 | grep -E "^(Scanning|Updating)" || true
          ./scripts/feeds install -a -f 2>&1 | tail -3 || true

      - name: 下载基础配置并合并
        run: |
          cd $SOURCE_DIR
          echo "📥 下载基础配置..."
          
          curl -fsSL "$CONFIG_URL" -o base.config || {
            echo "⚠️ 远程配置下载失败，使用本地"
            [ -f "defconfig/mt7987_mt7992.config" ] && cp defconfig/mt7987_mt7992.config base.config
          }
          
          cat base.config > .config
          if [ -f "$GITHUB_WORKSPACE/h5000m.config" ]; then
            cat "$GITHUB_WORKSPACE/h5000m.config" >> .config
          fi
          echo "✅ 配置已合并"

      - name: 应用插件配置
        run: |
          cd $SOURCE_DIR
          
          # 验证互斥选项
          if [ "${{ inputs.enable_qmodem_next }}" = "true" ] && [ "${{ inputs.enable_qmodem }}" = "true" ]; then
            echo "❌ 错误: 不能同时启用 QModem Next 和 QModem！"
            exit 1
          fi
          
          enable_pkg() { echo "CONFIG_PACKAGE_$1=y" >> .config; }
          disable_pkg() { echo "# CONFIG_PACKAGE_$1 is not set" >> .config; }
          
          # AdGuardHome
          [ "${{ inputs.enable_adguardhome }}" = "true" ] && {
            enable_pkg "luci-app-adguardhome"
            enable_pkg "adguardhome"
          } || {
            disable_pkg "luci-app-adguardhome"
            disable_pkg "adguardhome"
          }
          
          # HomeProxy
          [ "${{ inputs.enable_homeproxy }}" = "true" ] && enable_pkg "luci-app-homeproxy"
          
          # OpenClash
          if [ "${{ inputs.enable_openclash }}" = "true" ]; then
            enable_pkg "luci-app-openclash"
            enable_pkg "bash" "dnsmasq-full" "curl" "ca-bundle" "ip-full"
            enable_pkg "kmod-nft-tproxy" "luci-compat"
          else
            disable_pkg "luci-app-openclash"
            disable_pkg "kmod-nft-tproxy" "ip-full"
          fi
          
          # Mihomo
          [ "${{ inputs.enable_mihomo }}" = "true" ] && {
            enable_pkg "mihomo"
            enable_pkg "luci-app-mihomo"
          }
          
          # UPnP
          [ "${{ inputs.enable_upnp }}" = "true" ] && enable_pkg "luci-app-upnp" || disable_pkg "luci-app-upnp"
          
          # VLMCSd
          if [ "${{ inputs.enable_vlmcsd }}" = "true" ]; then
            enable_pkg "vlmcsd" "luci-app-vlmcsd"
          else
            disable_pkg "vlmcsd" "luci-app-vlmcsd"
          fi
          
          # SmartDNS
          [ "${{ inputs.enable_smartdns }}" = "true" ] && {
            enable_pkg "smartdns" "luci-app-smartdns"
          }
          
          # MosDNS
          if [ "${{ inputs.enable_mosdns }}" = "true" ]; then
            enable_pkg "mosdns" "luci-app-mosdns"
          else
            disable_pkg "mosdns" "luci-app-mosdns"
          fi
          
          # DockerMan
          [ "${{ inputs.enable_dockerman }}" = "true" ] && {
            enable_pkg "docker" "luci-app-dockerman"
          }
          
          # QModem
          if [ "${{ inputs.enable_qmodem_next }}" = "true" ]; then
            enable_pkg "luci-app-qmodem-next"
          elif [ "${{ inputs.enable_qmodem }}" = "true" ]; then
            enable_pkg "luci-app-qmodem"
          fi
          
          # MWAN
          [ "${{ inputs.enable_mwan }}" = "true" ] && {
            enable_pkg "mwan3" "luci-app-mwan3"
          }
          
          make defconfig || true
          echo "✅ 插件配置已应用"

      - name: 验证配置
        run: |
          cd $SOURCE_DIR
          echo "🔍 验证配置..."
          check_pkg() {
            grep -q "CONFIG_PACKAGE_$1=y" .config && echo "✅ $2: 启用" || echo "⚠️  $2: 禁用/未配置"
          }
          check_pkg "luci-app-adguardhome" "AdGuardHome"
          check_pkg "luci-app-openclash" "OpenClash"
          check_pkg "docker" "Docker"

      - name: 编译固件 (多线程)
        run: |
          cd $SOURCE_DIR
          THREADS=$(($(nproc) + 1))
          export PATH="/usr/lib/ccache:$PATH"
          
          echo "🔨 [编译阶段] 使用 $THREADS 线程编译..."
          START_TIME=$(date +%s)
          
          if make -j$THREADS BUILD_LOG=1 IGNORE_ERRORS=m 2>&1 | tee /tmp/build.log | tail -30; then
            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            echo "✅ 编译成功 (耗时: ${DURATION}s)"
          else
            echo "⚠️ 并行编译失败，尝试单线程编译..."
            if make -j1 V=s 2>&1 | tee /tmp/build_single.log | tail -50; then
              echo "✅ 单线程编译成功"
            else
              echo "❌ 编译失败"
              tail -100 /tmp/build_single.log
              exit 1
            fi
          fi

      - name: 保存编译缓存
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.SOURCE_DIR }}/staging_dir/host
            ${{ env.SOURCE_DIR }}/staging_dir/toolchain-*
            ${{ env.SOURCE_DIR }}/staging_dir/target-*
          key: ${{ runner.os }}-build-${{ env.REPO_BRANCH }}-${{ needs.download.outputs.cache-key }}

      - name: 上传编译结果
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: build-result-${{ github.run_number }}
          path: ${{ env.SOURCE_DIR }}/bin
          retention-days: 1

  # ============================================================================
  # Job 4: 打包和发布 (5-10 分钟)
  # ============================================================================
  package:
    runs-on: ubuntu-22.04
    needs: build
    if: success()
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 下载编译结果
        uses: actions/download-artifact@v4
        with:
          name: build-result-${{ github.run_number }}
          path: ${{ env.SOURCE_DIR }}/bin

      - name: 打包产物
        run: |
          mkdir -p artifacts
          cd $SOURCE_DIR
          
          if [ ! -d "bin/targets" ]; then
            echo "❌ 未找到编译输出"
            exit 1
          fi
          
          echo "📦 收集产物..."
          find bin/targets -type f \( -name "*.bin" -o -name "*.img.gz" \) -exec cp {} $GITHUB_WORKSPACE/artifacts/ \; 2>/dev/null || true
          
          if [ -z "$(ls -A $GITHUB_WORKSPACE/artifacts/ 2>/dev/null)" ]; then
            echo "❌ 没有找到产物文件"
            exit 1
          fi
          
          echo "✅ 产物收集完成:"
          cd $GITHUB_WORKSPACE/artifacts
          ls -lh

      - name: 生成产物清单
        run: |
          cat > $GITHUB_WORKSPACE/artifacts/MANIFEST.txt << 'EOF'
          ╔════════════════════════════════════════════════╗
          ║    ImmortalWrt H5000M 固件构建信息             ║
          ╚════════════════════════════════════════════════╝
          
          📅 构建时间: $(date '+%Y-%m-%d %H:%M:%S')
          🔗 运行 ID: ${{ github.run_id }}
          🏷️  Build #: ${{ github.run_number }}
          
          ⚙️ 启用的功能:
          EOF
          
          echo "  • AdGuardHome:  ${{ inputs.enable_adguardhome }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • HomeProxy:    ${{ inputs.enable_homeproxy }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • OpenClash:    ${{ inputs.enable_openclash }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • Mihomo:       ${{ inputs.enable_mihomo }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • UPnP:         ${{ inputs.enable_upnp }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • VLMCSd:       ${{ inputs.enable_vlmcsd }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • SmartDNS:     ${{ inputs.enable_smartdns }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • MosDNS:       ${{ inputs.enable_mosdns }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • DockerMan:    ${{ inputs.enable_dockerman }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • QModem Next:  ${{ inputs.enable_qmodem_next }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • QModem:       ${{ inputs.enable_qmodem }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "  • MWAN:         ${{ inputs.enable_mwan }}" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          
          echo "" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          echo "📦 产物文件:" >> $GITHUB_WORKSPACE/artifacts/MANIFEST.txt
          cd $GITHUB_WORKSPACE/artifacts
          ls -lh | grep -v "^total" | awk '{print "  • " $9 " (" $5 ")"}' >> MANIFEST.txt

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-H5000M-${{ github.run_number }}
          path: artifacts/
          retention-days: 30

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          tag_name: v${{ github.run_number }}
          name: ImmortalWrt H5000M #${{ github.run_number }}
          body: |
            🎉 **ImmortalWrt H5000M 固件已构建完成**
            
            📅 时间: ${{ github.event.repository.updated_at }}
            
            **⚙️ 启用的功能:**
            - AdGuardHome: ${{ inputs.enable_adguardhome }}
            - OpenClash: ${{ inputs.enable_openclash }}
            - Mihomo: ${{ inputs.enable_mihomo }}
            - MosDNS: ${{ inputs.enable_mosdns }}
            - Docker: ${{ inputs.enable_dockerman }}
            - MWAN: ${{ inputs.enable_mwan }}
            
            💾 下载产物: 见下方 Artifacts
            📋 详细信息: 见 MANIFEST.txt
          prerelease: false
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}